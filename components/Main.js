import styled from 'styled-components';

import { gutters } from '../style-utils/constants';

export default styled.main`
  margin: 0 auto;
  max-width: 1200px;
  padding-left: ${gutters.small}px;
  padding-right: ${gutters.small}px;
`;
